apiVersion: v1
kind: Service
metadata:
  name: llm-gateway
  namespace: freeciv
  labels:
    app: llm-gateway
    component: game-integration
spec:
  type: ClusterIP
  selector:
    app: llm-gateway
  ports:
    - name: websocket
      port: 8003
      targetPort: websocket
      protocol: TCP
