apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: freeciv
  labels:
    app.kubernetes.io/part-of: freeciv
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
